---
status: current
layer: domain
domain: settings-preferences
canonical_for:
  - settings-preferences-data-model
related:
  - docs/current/domains/download-engine/api-contract.md
  - docs/current/domains/settings-preferences/api-contract.md
last_verified: 2026-09-18
---

# settings-preferences 数据模型

## Purpose

给出 `Config` 与 `Preferences` 的完整字段表（含默认值、允许值与消费方），是新增设置项时的唯一对照表。
Rust 定义：`src-tauri/src/state/config_models.rs`、`preferences_models.rs`；前端定义：`src/tauri/types/config.ts`、`preferences.ts`。

## Current Behavior

### Config（`config.store.json` → 键 `config`）

> **2026-09-18 起**：应用只做「转录 + 翻译」。`Config` 当前形态为 `appearance`/`auth`/`network`/`input`/`performance`/`transcription`/`translation`/`logging`/`output`/`update`/`system`/`notifications`；`output` 的下载向字段与 `subtitles`/`sponsorBlock`/`inputFilters`/`input.preferVideoInMixedLinks` 已删除（旧文件中的未知键被忽略）。当前字段的权威说明见 `docs/current/domains/transcribe-translate/data-model.md`；本文件下方的下载向小节为历史记录，待归档。

服务端默认值见 Rust `Default` 实现；前端 `defaultSettings` 是 UI 侧镜像（`maxConcurrency` 前端为 `1`，后端为 CPU 核数一半向上取整——以**后端为准**，前端只用于首屏渲染）。

#### appearance

| 字段 | 类型 | 默认 | 允许值 | 消费方 |
| --- | --- | --- | --- | --- |
| `theme` | `string` | `"system"` | `system` / `light` / `dark` | `useTheme`（`data-theme` 属性） |
| `language` | `string` | `"system"` | `system` 或 locale 代码 | vue-i18n、Rust `I18nManager`、托盘/通知 |
| `expandedOptions` | `string` | `"none"` | `none` / `encodings` / `tracks` | 历史字段（configure 步骤已移除，保留以兼容旧配置） |

#### auth（Cookie 部分；密码类见 auth-secrets）

| 字段 | 类型 | 默认 | 说明 |
| --- | --- | --- | --- |
| `cookieFile` | `string \| null` | `null` | `--cookies` 路径 |
| `cookieBrowser` | `string` | `"none"` | `--cookies-from-browser` 值；`none` 表示不用 |

#### network

| 字段 | 类型 | 默认 | 说明 |
| --- | --- | --- | --- |
| `enableProxy` | `boolean \| null` | `null` → 首次启动按 `proxy` 是否非空推断 | 是否使用代理 |
| `proxy` | `string \| null` | `null` | 代理 URL |
| `impersonate` | `string` | `"none"` | TLS 指纹模拟目标（可选值见 `helpers/network.ts`） |
| `extractorArgs` | `string` | `""` | 多行 `--extractor-args`，按行连接 |

#### input

| 字段 | 类型 | 默认 | 说明 |
| --- | --- | --- | --- |
| `autoFillClipboard` | `boolean` | `true` | 是否允许剪贴板监听功能（UI 开关的初始可用性） |
| `preferVideoInMixedLinks` | `boolean` | `false` | 混合链接优先单视频 → `--no-playlist` |
| `globalShortcuts` | `boolean` | `true` | 是否注册全局快捷键 |

#### inputFilters（全局默认过滤，入队时转为 override）

| 字段 | 类型 | 默认 | 说明 |
| --- | --- | --- | --- |
| `minSize` / `maxSize` | `{ value: number \| null, unit: 'B'\|'KB'\|'MB'\|'GB'\|'TB' \| null }` | `value=null, unit='MB'` | 体积过滤，序列化为 `10M` 形式 |
| `dateFilter` | `{ mode: 'exact'\|'before'\|'after' \| null, value: 'YYYY-MM-DD' \| null }` | 均为 `null` | 转换为 `--date`/`--datebefore`/`--dateafter` |
| `matchFilters` / `breakMatchFilters` | `string \| null` | `null` | yt-dlp 过滤表达式 |

#### output

| 字段 | 类型 | 默认 | 说明 |
| --- | --- | --- | --- |
| `video.container` | `'mp4' \| 'mkv'` | `mp4` | 目标容器 |
| `video.policy` | `'never' \| 'allowReencode'` | `allowReencode` | 是否允许重编码（`never` = 保持原始流） |
| `video.postprocessPreset` | `'none' \| 'fps30' \| 'mp42' \| 'custom'` | `none` | 视频后处理预设 |
| `video.customPostprocessMode` | `'remux' \| 'reencode'` | `remux` | 自定义参数的作用方式 |
| `video.postprocessArgs` | `string` | `""` | 自定义 ffmpeg 参数（shlex 校验） |
| `audio.format` | `mp3\|m4a\|opus\|aac\|ogg\|flac\|wav` | `mp3` | `--audio-format` |
| `audio.policy` | 同视频 | `allowReencode` | 决定是否 `-x` 后转码 |
| `audio.postprocessPreset` / `postprocessArgs` | `'none'\|'custom'` / `string` | `none` / `""` | 自定义音频 ffmpeg 参数 |
| `addMetadata` | `boolean` | `true` | `--add-metadata` |
| `addThumbnail` | `boolean` | `true` | 嵌入缩略图（受容器/格式支持性限制） |
| `saveThumbnail` | `boolean` | `false` | `--write-thumbnail` |
| `preciseCuts` | `boolean` | `false` | SponsorBlock/部分下载的 `--force-keyframes-at-cuts` |
| `reversePlaylistNumbering` | `boolean` | `false` | 影响 `playlist_index` 计算（前端） |
| `downloadDir` | `string \| null` | 首次启动填系统下载目录 | 全局下载目录（组级/偏好可覆盖） |
| `fileNameTemplate` | `string` | `%(title).200s-(%(height)sp%(fps).0d).%(ext)s` | 视频文件名模板 |
| `audioFileNameTemplate` | `string` | `%(title).200s-(%(abr)dk).%(ext)s` | 音频文件名模板 |
| `restrictFilenames` | `boolean` | `false` | `--restrict-filenames` |

#### performance

| 字段 | 类型 | 默认 | 说明 |
| --- | --- | --- | --- |
| `maxConcurrency` | `number` | `ceil(CPU/2)`（最小 1） | 抓取与下载各自的信号量上限 |
| `splitPlaylistThreshold` | `number` | `50` | 播放列表拆分/合并阈值 |
| `autoLoadSize` | `boolean` | `true` | 卡片是否自动请求所选格式体积 |

#### sponsorBlock

| 字段 | 类型 | 默认 | 说明 |
| --- | --- | --- | --- |
| `apiUrl` | `string \| null` | `null` | 自定义 API |
| `removeParts` | `string[]` | `[]` | `--sponsorblock-remove` |
| `markParts` | `string[]` | `[]` | `--sponsorblock-mark` |

#### subtitles

| 字段 | 类型 | 默认 | 说明 |
| --- | --- | --- | --- |
| `enabled` | `boolean` | `false` | 总开关（仍需 inventory 非空） |
| `includeAutoGenerated` | `boolean` | `false` | 是否包含自动字幕 |
| `languages` | `string[]` | `["en"]` | 请求语言；`all` 表示全部 |
| `formatPreference` | `string[]` | `["srt","vtt","ass","ttml","json3"]` | 首选格式在首位 |
| `embedSubtitles` | `boolean` | `true` | 嵌入 vs 独立文件 |

#### update

| 字段 | 类型 | 默认 | 说明 |
| --- | --- | --- | --- |
| `updateBinaries` | `boolean` | `true` | 前端是否执行 `binaries_check`（toolchain 门控） |
| `updateApp` | `boolean` | `true` | 前端是否执行 `updater_check` |

#### system

| 字段 | 类型 | 默认 | 说明 |
| --- | --- | --- | --- |
| `trayEnabled` | `boolean` | `false` | 是否创建托盘图标 |
| `autoStartEnabled` | `boolean` | `false` | 是否开机自启（`--auto-start`） |
| `autoStartMinimised` | `boolean` | `false` | 自启动时不显示主窗口 |
| `closeBehavior` | `'exit' \| 'hide'` | Windows/Linux `exit`，macOS `hide` | 关闭窗口行为（仅在托盘开启时生效；macOS 始终隐藏） |

#### notifications

| 字段 | 类型 | 默认 | 说明 |
| --- | --- | --- | --- |
| `notificationBehavior` | `'always' \| 'onBackground' \| 'never'` | `never` | 通知时机；`onBackground` 仅在窗口不可见且未聚焦时弹（`force=true` 例外） |
| `disabledNotifications` | `NotificationKind[]` | `[]` | 逐项禁用；匹配则直接返回 |

### Preferences（`preferences.store.json` → 键 `preferences`）

#### paths

| 字段 | 类型 | 默认 | 说明 |
| --- | --- | --- | --- |
| `audioDownloadDir` | `string \| null` | `null` | 音频下载目录（覆盖 `output.downloadDir`） |
| `videoDownloadDir` | `string \| null` | `null` | 视频下载目录 |
| `audioDirectoryTemplate` | `string` | `""` | 音频子目录模板（拼在目录之后、文件名之前） |
| `videoDirectoryTemplate` | `string` | `""` | 视频子目录模板 |

#### recents

| 字段 | 类型 | 默认 | 说明 |
| --- | --- | --- | --- |
| `recent` | `Record<string, string[]>` | `{}` | 按 label 分组的最近路径，每个 label 最多 5 条（前端截断） |

#### formats

| 字段 | 类型 | 默认 | 说明 |
| --- | --- | --- | --- |
| `trackType` | `'both' \| 'audio' \| 'video'` | `both` | 上次选择的下载类型（用于新会话默认值） |

#### window

| 字段 | 类型 | 默认 | 说明 |
| --- | --- | --- | --- |
| `width` / `height` | `number` | `0` | 物理像素尺寸；为 0 表示未记录 |
| `x` / `y` | `number` | `0` | 普通状态位置 |
| `prevX` / `prevY` | `number` | `0` | 最大化前的位置（恢复最大化时使用） |
| `maximized` | `boolean` | `false` | 是否最大化 |

### 覆盖优先级（历史：下载相关，转录流程不适用）

```text
组级 DownloadOverrides  >  Preferences.paths（仅目录/目录模板）  >  Config  >  yt-dlp 默认
```

## Boundaries

- `Config` 中不含任何密钥；`AuthSettings` 只有 Cookie 路径与浏览器名。
- Rust 侧 `#[serde(default)]` + `materialize` 深合并保证旧版本配置文件可直接升级（缺失字段补默认值）。
- 前端 `Settings` 类型与 Rust `Config` 必须保持字段一一对应（同一 camelCase 序列化契约）。

## Contracts

- 命令：`api-contract.md`。
- 消费方细节：`../download-engine/api-contract.md`、`../media-queue/data-model.md`、`../app-lifecycle/backend-behavior.md`。

## Failure And Edge Cases

- `defaultPerformanceSettings.maxConcurrency = 1` 是前端占位值；真实默认值由后端在 `config_get` 时给出。
- `ProxySettings.enableProxy` 为 `null` 时后端 `before_initialized` 会写成布尔值；前端类型允许 `null` 以兼容首屏。
- `subtitleSettings.languages` 含 `all` 时前端 `sanitizeSubtitleLanguages` 会归一化成 `["all"]`（丢弃其它语言）。
- `preferences.formats.trackType` 与 `optionsStore` 的"全局选项"是两套东西：前者仅作为新会话默认值，后者是会话内共享。

## Verification

见 `docs/current/domains/settings-preferences/verification.md`；schema 单测参考 `tests/unit/settingsView.spec.ts`、`networkSettings.spec.ts`、`subtitleSettings.spec.ts`、`postprocessSettings.spec.ts`。

# Worklog: batch-transcribe-translate

<!--
Column contract (do not invent alternatives):
- 验证结果: report what was run and the outcome. Distinguish TASK-RELEVANT failures
  from a KNOWN pre-existing broken baseline. Tag an unrelated chronically-red check
  as `[quarantined: <reason + tracking ref>]`. Never let a quarantined signal
  silently absorb a new failure.
- 评审结论: `pending review` / `PASS` / `PASS_WITH_WARNINGS` / `FAIL` — the same
  value as the matching review Result.
- Commit: a landed loop carries the real short hash; `not committed` only while the
  work is intentionally uncommitted. A loop claiming PASS must be tied to a commit.
- Re-anchor: required on L004 / L007 / L010 / … (re-read original ask, design scope,
  verification ACs, active domain docs; value = on-track / scope-drift / intent-drift /
  doc-drift + one line). Other rows are `n/a`.
-->

实现循环已开始。第 1 个循环 L001 于 2026-09-17 执行（Phase A 第 1–2 项），行见下表。
开发者要求**先准备好全部文档、暂不实施**的阶段已结束（开发者下达开工指令后进入实施）；
后续循环按 tasks.md 的 Phase A/B/C 推进，L004 起按契约做 re-anchor。

2026-09-17 追加需求：长链路必须有 `.log` 文件（关键事件 + 错误，便于即时排查）。已增量评审通过，落入 design 第 8 节 + Phase A 第 11 项 + AC-21–AC-27；实现时事件码必须与 design 表逐字一致。

| Loop | 本轮目标 | 主要改动 | 验证结果 | 评审结论 | 下一轮动作 | 风险/待评估点 | Commit | Re-anchor |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| L001 | Phase A 第 1–2 项：配置 schema 增量 + `output.rootDir` 默认值 + stronghold `ai.apiKey` + 兼容性单测 | `src-tauri/src/state/config_models.rs`（新增 `transcription`/`translation`/`logging` 与 `output.rootDir`/`output.overwrite`；下载向字段按约定暂留）；`src-tauri/src/state/config.rs`（`apply_path_defaults` + `before_initialized` 填 rootDir；AC-18 单测 ×2）；`src-tauri/src/stronghold/stronghold_state.rs`（`AI_API_KEY` + 不实现 Debug/Display 的 `ApiKey` 包装 + `load_ai_api_key` + 单测）；`src/tauri/types/config.ts`（类型与 `defaultSettings` 镜像）、`tests/unit/configDefaults.spec.ts`（前端默认值镜像测试） | 通过：`npm run lint:fix`（无改动）；`npm run test:unit` 32 files / 129 tests；`npm run test:e2e` 21 passed（首次因缺 Playwright 浏览器失败，执行 `npx playwright install chromium` 后通过——属环境基线，非本循环引入）；`npm run build`（`vue-tsc --noEmit` app + isolation）；`python docs/scripts/check_doc_runtime.py docs` 仅遗留 1 条“worklog 无循环行”WARN（本行已消除）。Rust：[quarantined: 本机无 Rust 工具链（PATH、`~/.cargo`、scoop 及常见安装位置均无 `cargo`/`rustc`/`rustup`），`cargo fmt/clippy/test` 无法执行，待工具链可用后补跑] | pending review | L002 = Phase A 第 3 项（`runners/ytdlp_args/audio_args.rs`：`-f ba/best` + playlist 开关 + argv 单测），并评估 tasks 第 11 项（文件日志）是否提前；L003 后按评审节奏补 `reviews/L003.md`（含 L001 追溯） | Rust 侧未编译：枚举 serde 值（`small`/`large-v3`/`cuda`/`auto`）、`#[allow(dead_code)]` 的临时豁免范围、rustfmt 形制需在工具链可用后复核；前后端默认值靠后端单测与前端镜像手工同步，后续改默认值必须同时改 `config_models.rs` 与 `config.ts` | b89a3d0 | n/a |
| L002 | Phase A 第 3 项：音频下载 argv（AC-02）+ network/auth 参数构造去重（为保编译，旧 args 模块删除延后） | `runners/ytdlp_args/audio_args.rs`（新：`build_audio_download_args` = `-f ba/best` + `-o` + playlist + 网络/认证，沿用 `override_resolver` 三态合并）；`runners/ytdlp_args/network_args.rs`、`runners/ytdlp_args/auth_args.rs`（新：纯函数，自 `YtdlpRunner` 抽出，argv 顺序与旧实现逐一等价）；`runners/ytdlp_runner.rs`（`with_network_args`/`with_auth_args` 改为委托；删 `apply_auth_secrets`/`normalize_extractor_args`）；`runners/ytdlp_args/tests.rs`（AC-02 完整 argv + 禁用 flag 清单 + playlist/override 分支） | 通过：前端未改动但回归全绿（`lint:fix`、`test:unit` 32 files / 129 tests、`test:e2e` 21 passed、`build` 含 `vue-tsc` ×2）；`check_doc_runtime` 0 warning。Rust：[quarantined: 本机无 Rust 工具链，`cargo fmt/clippy/test` 无法执行；已人工比对参数顺序与旧实现等价 + 括号/分隔符平衡检查] | pending review | L003 = Phase A 第 11 项（文件日志：`tracing-appender` 文件层 + 事件码常量 + 脱敏 + 5MB×5 轮转，AC-21–AC-26），为后续 runner 的事件日志打底 | Rust 未编译；`build_audio_download_args` 暂挂 `#[allow(dead_code)]`（接线后必须删除）；playlist 语义暂沿用现网 `inputFilters.playlistMode` 与 `input.preferVideoInMixedLinks`，Phase B/C 删这些字段时需同步改 `audio_args::playlist_flag` | c18ccf6 | n/a |
| L003 | Phase A 第 11 项：文件日志（事件码 + 单行格式 + 集中脱敏 + 5MB×5 轮转 + `logging.verbose`），AC-21–AC-26 | `src-tauri/src/logging/events.rs`（新：35 个事件码常量 + 逐字断言；含新增 `tool.output`）；`src-tauri/src/logging/file_log.rs`（新：`FileLogFormat` 固定字段序/折叠控制字符/逐字段截断（title 80、excerpt 500、tail 2KB、默认 8KB）/敏感字段 `[redacted]`；`SizeRotatingFile`：逐行单次 `write(2)` + 5MB×5 轮转 + 一次性 `log.write_failed` 降级；`log_tool_output(verbose,…)`；`log_dir/log_file_path/log_size_bytes` 供 probe）；`src-tauri/src/logging/mod.rs`；`src-tauri/src/lib.rs`（fmt/Sentry 之外的第三层 + `file_log::configure(app_dir)` 惰性目录 + Sentry 排除 `ovd::tool_output`）；`src-tauri/Cargo.toml`（`tracing-subscriber` 增 `time`/`local-time` feature，无新增依赖） | 通过：前端回归全绿（`lint:fix`、`test:unit` 32/129、`test:e2e` 21 passed、`build` 含 `vue-tsc` ×2）；`check_doc_runtime` 0 warning。Rust：[quarantined: 本机无 Rust 工具链；`cargo fmt/clippy/test` 无法执行；已做括号平衡/行宽/`unwrap()`・`expect()`・`panic!` 扫描与事件码逐字比对] | pending review | L004 = Phase A 第 4–5 项（`runners/ffmpeg_runner.rs`、`runners/whisper_runner.rs`，强制复用 `ytdlp_process.rs` 进程封装以关 W001）；开跑前按契约做 re-anchor（原始需求 + design 范围 + AC + 领域文档） | Rust 未编译：`local-time` feature 名与 `Layer::boxed`/`with_filter` API 需工具链可用后验证；`Cargo.lock` 未刷新（features 变化）；6 处 `#[allow(dead_code)]` 为未接线 API 的临时豁免，接线后必须删除；**设计偏差待确认**：无 `tracing-appender`、改用同步 `SizeRotatingFile`（见 design.md → Design Deviations） | 701b71c | n/a |
| L004 | Phase A 第 4–5 项：共享进程封装（关 W001）+ `ffmpeg_runner` + `whisper_runner`（AC-03/AC-04/AC-06/AC-20） | `runners/ytdlp_process.rs`（新增 `spawn_piped`/`ProcessEvent`/`PipedProcess`/`run_streaming`（watch 取消杀树）/`prepend_bin_dir_to_path`/`tail_excerpt`）；`runners/ytdlp_runner.rs`（spawn/reader 下沉，类型改为 re-export，`build_command` 改用 PATH 助手）；`runners/mod.rs`（新模块注册）；`runners/ffmpeg_runner.rs`（新：ffprobe 时长 argv+JSON 解析、固定边界切块 `-ss`/`-t -c copy`（无 silencedetect）、错误枚举）；`runners/whisper_runner.rs`（新：argv（AC-03/04）、stdout 段行解析、CUDA OOM/退出码映射、`<chunk>.json` 路径） | 通过：前端回归全绿（`lint:fix`、`test:unit` 32/129、`test:e2e` 21 passed、`build` 含 `vue-tsc`）；`check_doc_runtime` 0 warning。**Node 端口语义校验 21/21 通过**（把 argv/时间戳解析/日志格式化/轮转算法逐行照搬后在真实文件系统 + 对抗输入上执行；发现并修复 2 个真实缺口：`parse_clock_component` 接受 `inf`/`NaN`/负数、`end < start` 未拒绝，另将 U+2028/U+2029 纳入折叠；脚本已入库为 `scripts/port-checks.mjs`（当前 21/21），只证明算法语义）。Rust：[quarantined: 本机无 Rust 工具链；已做括号/行宽/panic 扫描，argv 与 AC-03/04/06 逐字段核对] | pending review | L005 = Phase A 第 6 项（`transcribe/{chunking,merge,transcript}.rs` 纯函数 + 单测，AC-05/AC-06/AC-07/AC-09） | 未编译：`tokio::select!` 取消路径、`spawn_piped` 重构、`Box<dyn Layer>`/feature 均需工具链验证；两 runner 模块级 `#[allow(dead_code)]` 待 L008 接线后删除；`-t`/`-to` 偏差已记 design.md；W001 可在 `reviews/L003/L004` 走查后关闭 | 105d514 | on-track：重读 raw-requirement Q1/Q4/Q11/Q15/Q17/Q18 与 design §2/§3、AC-03/04/06/20 一致，无范围漂移；ffmpeg 用 `-t` 替代 `-to` 已按契约记入 Design Deviations |
| L005 | Phase A 第 6 项：`transcribe/{chunking,merge,transcript}.rs` 纯函数 + 单测（AC-05/AC-06/AC-07/AC-09） | `src-tauri/src/transcribe/{mod,chunking,merge,transcript}.rs`（新）：`plan_chunks`（固定边界 `k*chunkMinutes`，`0` → 单块，并集严格 `[0,duration]`；拒绝 0/负/NaN/inf/>30 天，防超长分配）；`merge_segments`（offset 平移、稳定 id `{chunk}:{seg}`、按 start 排序、段数守恒；缺失块必报 `coverage_gaps`、总缺口 >2s 时逐块定位；`boundary_risks` 用块内坐标判 tail <0.3s / head ≤0.02s；恶意时间戳返回 `MergeError` 不 panic）；`format_english_transcript`（1.2s 间隔 → 700 渲染字符 → 3 个句末段且句末收尾三条机械规则，段间空行，跳过空白段，无 `translation::*` 引用）；`lib.rs` 注册 `mod transcribe`（模块级 `#[allow(dead_code)]`，L008 接线后删）；`scripts/port-checks.mjs` 增补三组镜像算法 23 条对抗校验；`eslint.config.js` 给 `scripts/**/*.mjs` 单独设 `ecmaVersion: latest`（原 2018 使 L004 入库的端口脚本无法解析、`npm run lint` 必红） | 通过：`npm run lint:fix`（0 error）、`test:unit` 32 files/129 tests、`test:e2e` 21 passed、`build`（`vue-tsc` ×2）；`node scripts/port-checks.mjs` 44/44；`check_doc_runtime` 0 warning。**真 Rust 实证（非 cargo）**：用 npm 上官方同 commit（`42212a5c4`）二进制（rustc 1.86.0-beta.3 + rustfmt 1.8.0-beta + clippy 0.1.86）搭临时 sysroot，对仅依赖 std 的三模块建独立 crate：`rustfmt --check` clean；`rustc --test --target wasm32-wasip1` + Node WASI **35/35 单测通过**；`clippy-driver -D warnings` **0 warning**。Rust：[quarantined: 整仓 `cargo fmt/clippy/test` 仍不可跑（无 cargo 与 MSVC 链接库）；wasm/std-only 通道不覆盖 tauri 依赖与主机构建，工具链到位后必须补跑]；**真实媒体干跑**（开发者指定 video `nIABz0Z4IRA`，后端级 L3 前置）：yt-dlp（mweb+EJS）→ffprobe 502.224s→真 Rust plan→ffmpeg `-t` 切块（180.001/180.002/142.224）→whisper small GPU（stdout 段行与 JSON 完全一致 68/67/33）→真 Rust merge（168 段/覆盖 502.224s/2 处边界告警）+ 原稿（28 段/8843 字符）；与整片单块转录词级相似度 0.9933，差异均为 ASR 抖动、边界句完整；缺失块路径报 `COVERAGE_GAP chunk=1 gap=180`（详见 implementation-notes §8） | pending review | L006 = Phase A 第 7 项（`translation/{blocks,validate,deepseek_client}` + 单测，AC-10/AC-11/AC-13） | 三个模块暂挂模块级 `#[allow(dead_code)]`（L008 接线后必须删）；**新发现**：整仓 `rustfmt --check` 在 L001–L004 的 5 个文件（`logging/file_log.rs`×5、`state/config.rs`×3、`runners/whisper_runner.rs`、`runners/ytdlp_process.rs`、`runners/ytdlp_args/tests.rs`）有 11 处格式偏差，需专门跑 `cargo fmt --all`；机械分段第 3 条按“以句末标点结尾的段数”计数（`...`/小数点不虚增），设计未细化且只影响空行位置；覆盖告警为块级定位 + 总缺口 >2s 触发，尾部静音可能产生 0.5s 级条目；**真实下载发现**：yt-dlp 2026.07+ 的 YouTube 需 JS runtime+EJS solver（无则 403），`--remote-components ejs:github` 与签名校验规则冲突，需 L008/Phase C 前定方案（已记 design Risks + implementation-notes §8） | 167404d | n/a |
| L006 | 补装完整 Rust 工具链并让真实门禁可跑（清 L001–L005 验证债）；原计划的 translation 顺延为 L007 | 工具链：rustup 默认 stable 1.98.1 + CI 固定版 1.94.1（clippy/rustfmt/llvm-tools），VS 2022 Build Tools VCTools（MSVC 14.44 + Windows SDK 10.0.26100）；Windows 门禁经 `%TEMP%\msvc.cmd`（最小 PATH + `vcvars64.bat`）避开 Git `link.exe` 重名与 JDK `api-ms-win-*` shim。修复：`logging/file_log.rs`（`FileLogFormat` 改 `Box<dyn FormatTime + Send + Sync>`、文件层泛型 `Layer<S>`、去掉 `Registry` import）；`build.rs`（comctl32 v6 manifest，修 `STATUS_ENTRYPOINT_NOT_FOUND`）；`runners/whisper_runner.rs`（`chunk_json_path` 追加 `.json` 而非 `with_extension`，并把参数收拢为 `WhisperChunkRequest`）；`state/config.rs`（AC-18 fixture 改 camelCase `fileNameTemplate`）；`parsers/ytdlp_error.rs`（`?`）；`Cargo.lock` 刷新（`time`/`local-time` → `num_threads`/`libc`）；`cargo fmt --all` 应用全仓（含 L001–L004 的 11 处偏差） | 通过：`cargo fmt --all -- --check`、`cargo clippy --all-targets -- -D warnings`、`cargo test`（**194 passed / 0 failed**，1.94.1 与 1.98.1 各跑一遍）；前端四项同轮重跑：`lint:fix` 0 error、`test:unit` 32 files/129 tests、`test:e2e` 21 passed、`build`（`vue-tsc` ×2）；`node scripts/port-checks.mjs` 44/44；`check_doc_runtime` 0 warning | pending review | L007 = Phase A 第 7 项（`translation/{blocks,validate,deepseek_client}` + 单测，AC-10/11/13）；**L007 是 re-anchor 检查点**，开跑前按契约重读 raw-requirement / design / AC / 领域文档 | CI 盲区：`rust-ci.yml` 仅 push/PR 到 `main` 且跑 ubuntu，`dev` 从未触发—— 4 个真实缺陷（日志层泛型、Windows manifest、chunk JSON 路径、AC-18 fixture）因此长期隐藏；是否加 Windows/PR 触发由开发者决定。本机门禁依赖临时 `msvc.cmd` 包装（已记 implementation-notes §7.2）；L003 的 `Cargo.lock` 遗留风险已关闭；工具链就绪后 L007 起不再有 `[quarantined]` | 4dc17c9..6c530a8 | n/a |
| L007 | Phase A 第 7 项：`translation/{blocks,validate,deepseek_client}` + 单测（AC-10/AC-11/AC-13） | `translation/mod.rs`（新，模块级 `#[allow(dead_code)]`，L008 接线后删）；`blocks.rs`：`plan_blocks`（≤`maxSegmentsPerBlock` 段且 ≤`maxCharsPerBlock` 字符，段落永不切分，0 上限钳为 1）、`parse_glossary`（首个 `=` 分割、空行/空项跳过）、`build_system_prompt`（角色+硬规则+可选去填充词+术语表+JSON 契约，集中一处）、`build_user_prompt`（前 2 段原文+译文上下文 + `[id] text`）、`BlockContext::before`；`validate.rs`：`validate_block_response`（缺/多余/乱序/空译/数量偏差分类，`deny_unknown_fields` 拒绝额外字段）、`extract_numbers`、`check_numeric_fidelity`（数字边界、千分位/全角/空格归一、术语表映射豁免）；`deepseek_client.rs`：`DeepseekClient`（120s 超时、JSON 模式、`{base}/chat/completions`、key 只进 `Authorization` 头）；重试：401/403→`AuthFailed` 不重试，429→`Retry-After`（≤60s）否则指数退避（1s 起、8s 封顶），5xx/超时/网络/契约失败可重试，其余 4xx→`RequestRejected` 不重试；尝试次数 = `maxRetries+1`；返回 items+usage；`lib.rs` 注册 `mod translation` | 通过：Rust 门禁（1.94.1 与 1.98.1 各一遍）`cargo fmt --all -- --check` 干净、`cargo clippy --all-targets -- -D warnings` 0 告警、`cargo test` **226 passed / 0 failed**（新增 32 条：blocks/validate/deepseek_client，含自建 TCP mock 的 401/429/5xx/契约重试用例）；前端四项：`lint:fix` 0 error、`test:unit` 32 files/129 tests、`test:e2e` 21 passed、`build`；`node scripts/port-checks.mjs` 44/44（脚本已冻结为 L003–L005 历史校验器）；`check_doc_runtime` 0 warning。未做真实 DeepSeek 调用（无 key，属 Phase C L3） | pending review | L008 = Phase A 第 8 项（`scheduling/transcribe_pipeline.rs` + `transcribe/artifacts.rs`：三限流器、阶段/进度事件、取消、原子写与跳过/续跑；并接线 translation/transcribe 模块后删除全部 `#[allow(dead_code)]`） | 新增 `RequestRejected`（非重试 4xx）的具体错误码由 L008 定（design 表只有 `deepseekServerError`）；`dropFillers` 目前只在 prompt 规则中体现，质量靠 L3 人工抽验；数字保真为告警级，汉字数字/四舍五入可能产生噪声；DeepSeek 真实字段与限流行为待 L3 真 key 验证（N001/N002 仍开放）；新增 translation 模块级 `#[allow(dead_code)]`，接线后必须删；`zh.blocks.json` 的原子写与断点续跑属 L008 | 1391596、e118761 | on-track：重读 `docs/source/batch-transcribe-translate/raw-requirement.md`（Q1 DeepSeek / Q3 抽样验收 / Q6 逐段对应 / Q7 无时间戳纯文本）与 design §5/Backend Behavior、AC-10/11/13；transcribe-translate 领域 current 文档 Phase C 才建，本期仍以 change 文档为准，无范围/意图漂移 |
| L008 | Phase A 第 8 项：`transcribe/artifacts.rs` + `scheduling/transcribe_pipeline.rs`（四阶段、三限流器、阶段/进度事件、取消、原子写与跳过/续跑、summary.md），AC-08/15/16/19 | `transcribe/artifacts.rs`（新）：`write_atomic`（`.tmp`→rename+sync）、`file_is_non_empty`/`load_segments`/`load_zh_blocks`（缺失/空/损坏一律视为缺失）、`OutputPaths`+`sanitize_directory_name`（非法字符/保留名/长度/空标题）、`plan_skips`（overwrite/exists/segments/blocks 组合）、`zh_blocks_complete`/`assemble_zh_transcript`/`total_usage`、`find_audio_file`（排除 `.part`）、`source.json` 标记 + `find_existing_output`（零网络跳过）；`scheduling/transcribe_pipeline.rs`（新）：批注册/结果回收/`render_summary`/`batch_summary` 事件，视频任务四阶段（downloadingAudio→transcribing→translating→writing），下载用 `build_audio_download_args`+`YtdlpRunner` 流式取数（取消杀进程树），时长→`plan_chunks`→`cut_chunk`→`transcribe_chunk`（stdout 段→`transcribe_progress`，日志事件逐字对齐 design 表）→`merge_segments`（coverage/boundary 告警）→en 原子写→`plan_blocks`/`translation` 逐块（`TranslateLimiter`，key 每块从 stronghold 读，数字保真告警，成功即原子写 `zh.blocks.json`）→zh 原子写→按 `keepAudio` 清理；`models/transcribe.rs`（新，IPC 载荷 + `VideoStatus`/`VideoErrorCode`）、`MediaFatalPayload::business` | 通过：Rust 门禁（1.94.1 与 1.98.1）`cargo fmt --all -- --check` 干净、`cargo clippy --all-targets -- -D warnings` 0 告警、`cargo test` **240 passed / 0 failed**（新增 14：artifacts 原子写/损坏降级/跳过组合/组装/标记，pipeline 汇总/错误码映射/whisper JSON/outcome）；前端四项全绿；`node scripts/port-checks.mjs` 44/44；`check_doc_runtime` 0 warning。**未跑**：AC-15/16/19 的 L2 联动（fake 二进制 + mock DeepSeek + 真实 FS）属 Phase C 第 18 项；本循环为 L1 + 编译/clippy | pending review | L009 = Phase A 第 9 项（`commands/transcription/{transcription_probe,transcribe_start}` + 隔离白名单 + 删除 `media_size`；接线 `TranscribeSender` 后删除 transcribe/translation/pipeline 的 `#[allow(dead_code)]`） | 跳过路径不发 `media_add`（卡片交给 L009 命令响应/Phase B）；标题重名会落到同一目录（设计固有风险，`source.json` 只能辅助识别）；`RequestRejected`（4xx）归 `deepseekServerError`；新增 Design Deviation：`.work/source.json` 标记（为满足 AC-14 零网络跳过）；L2 未跑 | 52b8570 | n/a |
| L009 | Phase A 第 9 项：`commands/transcription/{transcription_probe,transcribe_start}` + 隔离白名单 + 删除 `media_size` + 清理全部 `#[allow(dead_code)]`（AC-01/AC-14/AC-27） | `commands/transcription/transcription_probe.rs`（新）：探测 whisper 路径/版本（`pip show openai-whisper`）/CUDA（`nvidia-smi` 驱动级）/模型缓存（`~/.cache/whisper/<model>.pt`）/ffmpeg/ffprobe（bin_dir→PATH，Windows 试 `.exe/.cmd/.bat`）/apiKeyConfigured（只读 stronghold）/logDir·logFile·logSizeBytes（AC-27），并写 `probe.ok`/`probe.missing` 文件日志；`transcribe_start.rs`（新）：清洗 URL→非空校验→`whisper_found` 门禁（不 spawn，缺失 `Err("whisperMissing")`，AC-01）→每条 URL 生成 group/id + `ensure_group_running` → `TranscribeRequest::Batch` → 返回 groupId 列表 + `run.start` 日志；`commands/mod.rs`/`lib.rs` handler/`src-isolation/main.ts` 白名单同步；删除 `media_size`（命令、handler、白名单、`FetchRequest::Size/SizePlaylist`、`FetchEntry.format`、`MediaAddWithFormatPayload`、前端 `stores/media/size.ts`/`media.ts` 引用/`listeners/media.ts` 订阅/`types/media.ts` 类型/`MediaConfigureStep.vue` 体积 UI）；清理全部临时 `#[allow(dead_code)]`（transcribe/translation/pipeline/runners/ytdlp_process/ytdlp_args/logging/stronghold）并补齐暴露的真实缺口：`audio.download.fail`（exit/errCode）、`translate.block.retry`（attempt/status）、pipeline 改用 `plan_skips`/`ArtifactState` | 通过：Rust 门禁（1.94.1 与 1.98.1）`cargo fmt --all -- --check` 干净、`cargo clippy --all-targets -- -D warnings` 0 告警、`cargo test` **244 passed / 0 failed**（+4 probe 单测）；前端四项：`lint:fix` 0 error、`test:unit` 32 files/129 tests、`test:e2e` 21 passed、`build`（含 isolation 构建）；`node scripts/port-checks.mjs` 44/44；`check_doc_runtime` 0 warning。AC-13/14/15/16/19 的 L2 联动仍待 Phase C 第 18 项 | pending review | L010（**re-anchor 检查点**）= Phase A 第 10 项（`batchFinished` 通知前后端同步 + 汇总/清理收尾）+ 启动 Phase B；开跑前先补播放列表自动展开（design §2） | 播放列表链接当前直接报 `fetchFailed`（自动展开未实现，需在 Phase B 前补）；跳过路径不发 `media_add`（卡片交给 Phase B 命令响应/汇总）；体积相关 i18n 键与下载 UI 待 Phase B 整体清理；`transcribe_start` 未做 URL 合法性校验（设计把校验交给前端） | 42f59e5、9041062 | n/a |
| L010 | Phase A 第 10 项：播放列表自动展开（design §2）+ `batchFinished` 通知四处同步 + Phase A 收尾（取消清理） | `scheduling/transcribe_pipeline.rs`：新增 `TranscribeRequest::Expand`（fetch 阶段解析出播放列表后，在同一 group 内生成逐视频任务并追加进运行中的批次）、`expand_batch` 槽位替换计数（`remaining - 1 + N`）、`expand_playlist`（先发 `media_add` leader 再逐个发子项，写 `playlist.expand` 日志）、`playlist_children`（去空 URL、继承 group/batch、`total = 条目数`）、`run_video -> Option<VideoOutcome>`（链接任务不产生汇总行）、`FetchedMedia`/`fetch_metadata` 解析分支、`record_outcome` 批次归零后 `notify(BatchFinished)` + `batch_notification_params`；新根模块 `src-tauri/src/notifications.rs`（`NotificationKind` + 泛型 `notify<R: Runtime>`，`try_state` 缺失时静默跳过）并让 `commands/notifications.rs` 变薄壳、`state/config_models.rs` 改依赖该模块（去掉 state→commands 反向依赖）；`commands/group/group_cancel.rs` 向 `TranscribeSender` 发 `Cleanup`；`logging/events.rs` 增 `playlist.expand`（design 表先行）；`src/tauri/types/app.ts` 与两份 locale（13×2）同步 `batchFinished`；新增 7 条真实单测（展开顺序/固定 fixture JSON/批槽位/汇总归零+通知参数/通知 key 与两份 en locale 逐 kind 校验） | 通过：Rust 门禁（1.98.1 与 1.94.1 各一遍）`cargo fmt --all -- --check` 干净、`cargo clippy --all-targets -- -D warnings` 0 告警、`cargo test` **251 passed / 0 failed**（L009 基线 244 + 7）；前端四项：`lint:fix` 0 error、`test:unit` 32 files/129 tests、`test:e2e` 21 passed、`build`（`vue-tsc` ×2，含 isolation）；`node scripts/port-checks.mjs` 44/44；`check_doc_runtime` 0 warning。AC-13/14/15/16/19 的 L2 联动仍属 Phase C 第 18 项 | pending review | 下一步：先补 L001–L009（含本循环）的集中评审（`reviews/L0xx.md` + `reviews/index.md` 台账，关 W001/W005），再开 Phase B 第 11 项（MediaState/卡片步骤覆盖式改造） | 播放列表 = 1 group（链接）的语义与 `media_add.total = N` 需 Phase B 在 store 侧承接；队列中取消会让该批永不归零（W009，Phase C 修，见 implementation-notes §12.3）；跳过路径不发 `media_add`；YouTube EJS/JS runtime 方案未定；同标题目录冲突 | e571899 | on-track：重读 raw-requirement（Q1/Q4/Q6/Q7/Q11/Q15/Q17/Q18）、design §1/§2/§6/§7/§8 与 AC-01…AC-27；本循环内容（播放列表展开、批次通知、取消清理）均在设计内，`playlist.expand` 事件码与展开落点偏差已先写回 design 事件表/Design Deviations，无范围/意图漂移 |
| L011 | Phase B 第 12 项（`transcription` store + 事件监听）+ 第 11 项增量（`MediaState` 新状态、步骤组件与 stepMap）；`configure` 删除与首页输入改线留到第 13 项同轮（先删会让下载入口失效，你正在用它测试） | `src/tauri/types/transcription.ts`（新：`TranscribeStage`/stage/progress/artifact/summary/probe 的 IPC 字段镜像，camelCase 与 Rust 一致）；`src/stores/transcription.ts`（新：probe、阶段→`MediaState` 映射、块/翻译进度、artifact `{en,zh}`、逐 item 与分组 token 累计、`summaries`/`latestSummary`、`forgetItem`/`reset`；不持久化）；`src/tauri/listeners/transcription.ts`（新：`transcribe_stage`/`transcribe_progress`/`translate_progress`/`artifact_written`/`batch_summary` 五个监听）+ `plugins/tauriListeners.ts` 注册；`stores/media/state.ts` 增 `downloadingAudio`/`transcribing`/`translating`/`writing`；新步骤组件 `AudioDownloadStep`（indeterminate）/`TranscribeStep`（块进度 + 模型）/`TranslateStep`（块进度 + token），`MediaCard.vue` stepMap 接入四态（`writing` 复用 TranslateStep）；i18n `media.steps.{audioDownload,transcribe,translate}` 先补 `en`+`zh-CN`（其余语言第 17 项补齐）；`tests/unit/transcriptionStore.spec.ts`（新，5 条：阶段映射/进度钳位/token 累计/artifact 合并/分组聚合与 reset） | 通过：前端 `lint:fix` 0 error、`test:unit` 34 files/**138 tests**（+1 文件/+5 条）、`test:e2e` 21 passed、`build`（`vue-tsc` ×2 含 isolation）；Rust 本循环未改动但按契约重跑：`cargo fmt --check` 干净、`cargo clippy --all-targets -- -D warnings` 0 告警、`cargo test` **254 passed / 0 failed**（1.98.1 与 1.94.1）；`node scripts/port-checks.mjs` 44/44；`check_doc_runtime` 0 warning | pending review | L012 = Phase B 第 13 项（`/setup` 页 + whisper 门禁 + 首页输入改接 `transcribe_start` + playlist group 拆分）与第 11 项收尾（删除 `configure`/`MediaConfigureStep` 路径），随后第 14 项设置页签（含已定案的大模型配置） | 新步骤组件按 group 首个 item 取进度（playlist group 拆分后 1:1）；`writing` 无独立组件（复用 TranslateStep）；`transcribe_start` 尚未接线，新状态目前只能由事件触发（L012 接通）；音频下载无字节级进度；`media_complete` 仍走下载向的 VideoFinished 通知，留待第 14/15 项与批次通知一起收敛 | a3198e3 | n/a |
